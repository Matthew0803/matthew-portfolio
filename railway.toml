[build]
builder = "nixpacks"

[build.nixpacks]
packages = ["nodejs", "python3", "gcc", "g++", "make"]

[deploy]
startCommand = "npm start"
healthcheckPath = "/"
healthcheckTimeout = 300
restartPolicyType = "on_failure"
